images:
  base-cpu:
    system:
      - wget
    python: 3.8.10
    name: agipack
    pip:
      - scikit-learn
    run:
      - echo "Hello, world!"

  dev-cpu:
    base: base-cpu
    system:
      - build-essential